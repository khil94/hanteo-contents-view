export interface ICategory {
  name: string;
  url?: string;
}

export interface IBanner {
  url: string;
  imgUrl: string;
  isInProgress: boolean;
}

export interface IMockupData {
  url: string;
  thumbnailUrl: string;
  title: string;
}
